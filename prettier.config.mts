import type { Config } from "prettier";

const config: Config = {
  // ===== Основные настройки =====
  semi: true, // Ставить точку с запятой в конце выражений
  singleQuote: false, // Использовать двойные кавычки вместо одинарных (для строк в JS/TS)
  trailingComma: "all", // Висячая запятая везде, где это синтаксически возможно (объекты, массивы, аргументы функций) — уменьшает диффы в git при добавлении новых элементов
  printWidth: 100, // Максимальная длина строки, после которой Prettier переносит код — стандартные 80 тесноваты для TS с типами
  tabWidth: 2, // Количество пробелов на один уровень отступа
  useTabs: false, // Использовать пробелы для отступов вместо табуляции
  arrowParens: "always", // Всегда оборачивать единственный параметр стрелочной функции в скобки: (x) => x, а не x => x
  endOfLine: "lf", // Символ конца строки — LF (Unix), чтобы не было расхождений между macOS/Linux и Windows

  // ===== Прочие настройки (не заданы явно — оставлены как справка) =====
  // quoteProps: "as-needed", // Когда оборачивать ключи объекта в кавычки: "as-needed" (только когда обязательно) | "consistent" (все или ни одного) | "preserve" (как в исходнике)
  // jsxSingleQuote: false, // Использовать одинарные кавычки в JSX-атрибутах (актуально только при наличии JSX)
  // bracketSpacing: true, // Пробелы внутри фигурных скобок объектных литералов: { foo: bar } вместо {foo: bar}
  // bracketSameLine: false, // Ставить ли закрывающую скобку многострочного JSX-тега на той же строке, что и последний атрибут
  // arrowParens: "avoid", // Альтернатива "always" — не оборачивать одиночный параметр в скобки: x => x
  // proseWrap: "preserve", // Перенос строк в markdown-текстах: "preserve" (как есть) | "always" (по printWidth) | "never" (не переносить)
  // htmlWhitespaceSensitivity: "css", // Как учитывать пробелы в HTML при форматировании — влияет на переносы вокруг инлайновых тегов
  // vueIndentScriptAndStyle: false, // Добавлять отступ внутри <script> и <style> в .vue файлах
  // insertPragma: false, // Вставлять комментарий @format в начало файлов, обработанных Prettier
  // requirePragma: false, // Форматировать только файлы, где уже есть комментарий @format (для постепенного внедрения Prettier в большой кодовой базе)
  // rangeStart: 0, // Начало диапазона строк для форматирования (используется программным API, не CLI-конфигом)
  // rangeEnd: Infinity, // Конец диапазона строк для форматирования (аналогично rangeStart)
  // parser: "typescript", // Явное указание парсера — обычно Prettier сам определяет его по расширению файла
  // filepath: "", // Путь к файлу для автоопределения парсера (актуально при форматировании кода из stdin через API)
  // embeddedLanguageFormatting: "auto", // Форматировать ли код, встроенный в шаблонные строки (например CSS в styled-components) — "auto" | "off"
  // singleAttributePerLine: false, // Каждый атрибут JSX/HTML-тега на отдельной строке, даже если один
  // experimentalTernaries: false, // Экспериментальный формат вложенных тернарных операторов (более читаемый многострочный вид)
  // experimentalOperatorPosition: "end", // Экспериментальное расположение бинарных операторов при переносе строки — "end" | "start"
  // objectWrap: "preserve", // Перенос свойств объекта на новую строку — "preserve" (как в исходнике) | "collapse" (схлопнуть, если помещается)
  // plugins: [], // Список плагинов Prettier (например для поддержки дополнительных языков/синтаксисов)
  // overrides: [
  //   {
  //     files: "*.md",
  //     options: { proseWrap: "always" }, // Пример: свои настройки только для определённых файлов/расширений
  //   },
  // ],
};

export default config;
